import 'package:flutter/material.dart';
import 'package:flutter_ui_example/insta_like_anim/insta_like_anim_app.dart';

void main() {
  // runApp(const ParallaxApp());
  // runApp(const TransitionLocationApp());
  // runApp(const MovieApp());
  // runApp(const CollapseToolbarApp());
  runApp(const InstaLikeAnimApp());
}
