import 'package:flutter/material.dart';
import 'package:flutter_ui_example/collapse_toolbar/collapse_toolbar_app.dart';
import 'package:flutter_ui_example/movie/movie_app.dart';

void main() {
  // runApp(const ParallaxApp());
  // runApp(const TransitionLocationApp());
  // runApp(const MovieApp());
  runApp(const CollapseToolbarApp());
}
